import React from 'react';
import { shallow } from 'enzyme';
import { common } from '../../util/generic-tests';
import Tab from './Tab';
describe('Tab', () => {
	common(Tab);
	describe('Events', () => {
		describe('demo fn', () => {
			it('demo', () => {});
		});
	});
});
