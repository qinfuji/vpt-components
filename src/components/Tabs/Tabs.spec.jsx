import React from 'react';

import { common } from '../../util/generic-tests';
import Tabs from './Tabs';
import '../../index.less';

describe('Tabs', () => {
	common(Tabs);
	describe('Events', () => {
		describe('demo fn', () => {
			it('demo', () => {});
		});
	});
});
